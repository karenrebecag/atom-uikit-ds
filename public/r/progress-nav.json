{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "progress-nav",
  "type": "registry:component",
  "title": "ProgressNav",
  "description": "ProgressNav",
  "registryDependencies": [
    "tokens",
    "foundation"
  ],
  "files": [
    {
      "path": "components/atoms/ProgressNav.tsx",
      "type": "registry:component",
      "content": "import { forwardRef, type ReactNode } from 'react';\n\nexport type ProgressNavItem = {\n  id: string;\n  label: string;\n};\n\nexport type ProgressNavProps = {\n  items: ProgressNavItem[];\n  logo?: ReactNode;\n  cta?: {\n    label: string;\n    href: string;\n  };\n  topAnchor?: string;\n  bottomAnchor?: string;\n  className?: string;\n};\n\nfunction cn(...classes: (string | false | undefined | null)[]) {\n  return classes.filter(Boolean).join(' ');\n}\n\nfunction NavButton({ id, label }: { id: string; label: string }) {\n  return (\n    <a\n      data-progress-nav-target={`#${id}`}\n      href={`#${id}`}\n      className=\"progress-nav__btn\"\n    >\n      <span className=\"progress-nav__btn-text\">{label}</span>\n      <span className=\"progress-nav__btn-text is--duplicate\">{label}</span>\n    </a>\n  );\n}\n\nexport const ProgressNav = forwardRef<HTMLElement, ProgressNavProps>(\n  ({ items, logo, cta, topAnchor = 'top', bottomAnchor = 'bottom', className }, ref) => {\n    return (\n      <nav ref={ref} className={cn('progress-nav', className)}>\n        <div className=\"progress-nav__inner\">\n          {logo && (\n            <a href={`#${topAnchor}`} className=\"progress-nav__logo\">\n              {logo}\n            </a>\n          )}\n\n          <div className=\"progress-nav__wrapper\">\n            <div data-progress-nav-list=\"\" className=\"progress-nav__list\">\n              <div className=\"progress-nav__indicator\" />\n              <div\n                data-progress-nav-target={`#${topAnchor}`}\n                className=\"progress-nav__btn is--before\"\n              />\n              {items.map((item) => (\n                <NavButton key={item.id} id={item.id} label={item.label} />\n              ))}\n              <div\n                data-progress-nav-target={`#${bottomAnchor}`}\n                className=\"progress-nav__btn is--after\"\n              />\n            </div>\n          </div>\n\n          {cta && (\n            <a href={cta.href} className=\"progress-nav__contact-btn\">\n              <span className=\"progress-nav__btn-text\">{cta.label}</span>\n              <span className=\"progress-nav__btn-text is--duplicate\">{cta.label}</span>\n            </a>\n          )}\n        </div>\n      </nav>\n    );\n  },\n);\n\nProgressNav.displayName = 'ProgressNav';\n"
    },
    {
      "path": "styles/components/progress-nav.css",
      "type": "registry:file",
      "content": "/* ============================================================\n   ProgressNav — fixed one-page navigation with scroll indicator\n   Uses GSAP ScrollTrigger via @atom-uikit/animations\n   ============================================================ */\n\n.progress-nav {\n  width: 100%;\n  padding: var(--spacing-6);\n  position: fixed;\n  top: 0;\n  left: 0;\n  z-index: var(--z-index-30);\n}\n\n.progress-nav__inner {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  position: relative;\n}\n\n/* ---- Logo ---- */\n.progress-nav__logo {\n  color: inherit;\n  text-decoration: none;\n  display: flex;\n  align-items: center;\n}\n\n.progress-nav__logo-svg {\n  width: 8em;\n  height: auto;\n}\n\n/* ---- Pill wrapper (contains the nav list) ---- */\n.progress-nav__wrapper {\n  background-color: var(--muted);\n  border-radius: var(--radius-full);\n  padding: var(--spacing-1);\n}\n\n/* ---- Nav list ---- */\n.progress-nav__list {\n  border-radius: var(--radius-full);\n  display: flex;\n  justify-content: flex-start;\n  align-items: center;\n  position: relative;\n  overflow: hidden;\n}\n\n/* ---- Sliding indicator ---- */\n.progress-nav__indicator {\n  z-index: 2;\n  background-color: var(--card);\n  border-radius: var(--radius-full);\n  width: 2.5em;\n  height: 2.5em;\n  position: absolute;\n  left: -2.5em;\n  transition: all var(--duration-700) cubic-bezier(0.16, 1, 0.3, 1);\n  box-shadow: var(--shadow-xs);\n}\n\n/* ---- Nav buttons ---- */\n.progress-nav__btn {\n  z-index: 3;\n  cursor: pointer;\n  color: inherit;\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  height: 2.5em;\n  padding-left: 1em;\n  padding-right: 1em;\n  text-decoration: none;\n  position: relative;\n  overflow: hidden;\n}\n\n/* Hidden sentinel buttons (before/after) */\n.progress-nav__btn.is--before {\n  z-index: 1;\n  width: 2.5em;\n  height: 2.5em;\n  padding: 0;\n  position: absolute;\n  right: 100%;\n}\n\n.progress-nav__btn.is--after {\n  z-index: 1;\n  width: 2.5em;\n  height: 2.5em;\n  padding: 0;\n  position: absolute;\n  left: 100%;\n}\n\n/* ---- Text with hover slide animation ---- */\n.progress-nav__btn-text {\n  white-space: nowrap;\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  height: 100%;\n  font-size: var(--font-size-base);\n  font-weight: var(--font-weight-medium);\n  transition: transform var(--duration-500) cubic-bezier(0.16, 1, 0.3, 1);\n  transform: translateY(0%) rotate(0.001deg);\n}\n\n.progress-nav__btn-text.is--duplicate {\n  position: absolute;\n  top: 100%;\n}\n\n.progress-nav__btn:hover .progress-nav__btn-text,\n.progress-nav__contact-btn:hover .progress-nav__btn-text {\n  transform: translateY(-100%) rotate(0.001deg);\n}\n\n/* ---- CTA button ---- */\n.progress-nav__contact-btn {\n  color: var(--primary-foreground);\n  background-color: var(--primary);\n  border-radius: var(--radius-full);\n  height: 3.5em;\n  padding-left: var(--spacing-6);\n  padding-right: var(--spacing-6);\n  text-decoration: none;\n  display: flex;\n  align-items: center;\n  position: relative;\n  overflow: hidden;\n  transition: background-color var(--duration-200) var(--easing-in-out);\n}\n\n.progress-nav__contact-btn:hover {\n  background-color: var(--primary-hover);\n}\n\n/* ---- Reduced motion ---- */\n@media (prefers-reduced-motion: reduce) {\n  .progress-nav__indicator {\n    transition-duration: 0ms;\n  }\n  .progress-nav__btn-text {\n    transition-duration: 0ms;\n  }\n  .progress-nav__contact-btn {\n    transition-duration: 0ms;\n  }\n}\n"
    }
  ],
  "atom": {
    "discovery": {
      "name": "ProgressNav",
      "description": "ProgressNav",
      "category": "surfaces",
      "variants": [],
      "sizes": [],
      "defaultVariant": "",
      "defaultSize": "",
      "hasAnimation": true,
      "props": [
        {
          "name": "items",
          "type": "ProgressNavItem[]",
          "required": true
        },
        {
          "name": "logo",
          "type": "ReactNode",
          "required": false
        },
        {
          "name": "cta",
          "type": "{ label: string",
          "required": false
        },
        {
          "name": "href",
          "type": "string",
          "required": true
        }
      ]
    },
    "implementation": {
      "baseClass": "progress-nav",
      "cssClasses": [
        "progress-nav",
        "progress-nav__inner",
        "progress-nav__logo",
        "progress-nav__logo-svg",
        "progress-nav__wrapper",
        "progress-nav__list",
        "progress-nav__indicator",
        "progress-nav__btn",
        "progress-nav__btn-text",
        "progress-nav__contact-btn"
      ],
      "peerDeps": [
        "gsap"
      ],
      "hasCss": true,
      "hasReact": true
    }
  }
}